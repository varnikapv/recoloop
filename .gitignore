node_modules/
.next/
*.tsbuildinfo
next-env.d.ts

# Secrets — never commit. .env.example is the only env file that is tracked.
.env
.env.*
!.env.example
*.key
*.pem

# Reviewer decisions are runtime state, not a build artefact.
data/*/audit_log.jsonl
